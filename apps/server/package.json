{
  "name": "hyperliquid-mcp-server",
  "description": "MCP (Model Context Protocol) server for Hyperliquid",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build":"bun build src/index.ts --outdir dist"
  },
  "dependencies": {
    "hono": "^4.9.8"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/bun": "latest"
  }
}